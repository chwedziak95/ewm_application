import { InternalOrderItem } from './internal-order-item';

describe('InternalOrderItem', () => {
  it('should create an instance', () => {
    expect(new InternalOrderItem()).toBeTruthy();
  });
});
